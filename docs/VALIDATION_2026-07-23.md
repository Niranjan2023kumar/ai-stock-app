# VALIDATION REPORT — AI Stock Intelligence
*(2026-07-23, on-device Realme RMX3998 + 10-dimension adversarial source+screenshot audit. Market OPEN, PRACTICE mode, free Yahoo data — no Angel One keys on device.)*

**Result: 126 PASS-items confirmed · 28 findings CONFIRMED (survived adversarial verification) · 18 refuted (false positives dropped).**

Environment: new-PC build GREEN, 31 unit tests GREEN, installed to phone with data preserved, real data cross-checked (NIFTY matched Yahoo to the paisa).

Severity of confirmed findings: HIGH 1 · MEDIUM 13 · LOW 14

---
## CONFIRMED FINDINGS (fix candidates, most severe first)

### 1. [HIGH] FAIL — E5 / C13(I3) / B7b — U1.7, U9.1  _( settings-voice-keys )_
- **Defect:** Settings keeps a second, manually-editable money value ('Total Money You Have = 100000') that is never synced to the daily check-in, so the app shows the user two contradictory 'my money' figures and the Settings-displayed loss limits do not match what the app enforces.
- **Evidence:** SettingsScreen.kt:143-150 (editable capital field, value=state.capital via vm.updateCapital); default 100000 at SettingsViewModel.kt:12 + UserSettingsStore.kt:51; InterdayViewModel.kt:441,465-468 (check-in writes a DIFFERENT key daily_capital, never pref_capital) and :483-484,493 (effectiveCapital prefers daily_capital and ignores Settings capital); preset money-line SettingsScreen.kt:329-331; screenshots 02 ('My budget ₹1,000') vs 20 ('Total Money You Have 100000').
- **Failure scenario:** User taps the ₹1,000 check-in (screenshot 07), then opens Settings: it shows 'Total Money You Have ₹100000' and 'Normal — stops the day after about ₹3,000 lost'. But with a ₹1,000 budget the app actually caps the day at ~₹100 (1,000×3%, floored, InterdayViewModel:493). A zero-knowledge user is shown a money figure 100× their budget and a daily-loss promise 30× the real one, with no way to reconcile the two numbers — exactly the 'second money source' E5/C13 forbid.
- **Suggested fix:** Make 'My money' in Settings a read-only line sourced from the daily check-in (daily_capital), per E5 'auto-filled from the daily check-in B7b'; or have setDailyCapital also write pref_capital so both always agree. Remove the free-typed capital field to honor I3/C13 ('no second place to type money').
- **Verifier (CONFIRMED):** Verified against code, spec, and screenshots. Settings holds a SECOND, manually-typeable money field: SettingsScreen.kt:143-150 (NumberTextField "Total Money You Have (₹)", value=state.capital, onValueChange=vm.updateCapital) writing pref_capital, default 100000 (SettingsViewModel.kt:12 -> UserSettingsStore.DEFAULT_CAPITAL, UserSettingsStore.kt:51). The daily check-in writes a DIFFERENT key: InterdayViewModel.kt:441 daily_capital, set only by setDailyCapital (:465-468, called from HomeScreen.kt:882). A full-tree grep shows NO code syncs pref_capital<->daily_capital, and SettingsViewModel.init 

### 2. [MEDIUM] FAIL — D2 / B0.3 / F7  _( guide-moreways )_
- **Defect:** The entire D2 IPO feature is unimplemented — the IPO 'way' is a static card with no NSE listings, no dates, no plain-word prices, no affordability check, no APPLY/SKIP verdict, and no open-IPO notification.
- **Evidence:** MoreWaysScreen.kt:233-252 (IpoCard is hard-coded text: 'this app does not recommend them yet').
- **Failure scenario:** A user with enough money for a lot opens More Ways expecting the app to check 'does this open IPO fit my money — APPLY or SKIP' as D2 mandates; instead it flatly says it doesn't do IPOs, so a locked PART D earning avenue is entirely absent. The code invokes H14 to justify skipping it, but IPO alerts are a core PART D/B0.3/F7 requirement, not an H14 phase-2 item.
- **Suggested fix:** Implement D2 from NSE's free IPO listings: show open/upcoming IPOs with 'shares cost ₹X–₹Y each', check affordability against the stored capital, give a one-line APPLY/SKIP verdict (APPLY only when one lot fits), and fire the open-IPO notification (E1).
- **Verifier (CONFIRMED):** The D2 IPO feature is entirely unimplemented. IpoCard (MoreWaysScreen.kt:233-252) is a hard-coded static card with only two literal strings ("IPOs: new companies selling their shares for the first time." / "New-company shares are risky for beginners — this app does not recommend them yet.") — no NSE listings, no dates, no plain-word prices, no affordability check, no APPLY/SKIP verdict, no notification, not even a Groww button. MoreWaysViewModel.kt:50 explicitly states "the D2 IPO card [is] static (no market data)" and its load() fetches only Gold/Silver/US stocks. A case-insensitive grep for 

### 3. [MEDIUM] PARTIAL — D1 / U9.1 / I2  _( guide-moreways )_
- **Defect:** The gold card never explains in plain words that this is DIGITAL gold with no locker/shop — D1 mandates that exact reassurance; it only exposes the raw ticker 'GOLDBEES'.
- **Evidence:** MoreWaysViewModel.kt:101-105 (growwNote = 'In Groww it is called GOLDBEES.'); MoreWaysScreen.kt:294-297 renders only monthlyLine + growwNote, no 'digital gold, no locker, no shop' line.
- **Failure scenario:** A zero-knowledge user reads 'Gold ₹75 … In Groww it is called GOLDBEES … Buy in Groww', has no idea GOLDBEES is paper/digital gold held in a demat account, expects physical metal or is confused by the ticker jargon, and abandons the purchase.
- **Suggested fix:** Add D1's plain sentence 'digital gold you hold inside Groww — no locker, no shop' to the gold/silver card face, and keep 'Gold ETF / Sovereign Gold Bond' behind See more.
- **Verifier (CONFIRMED):** D1 (REQUIREMENTS_FINAL.md:164) explicitly mandates the gold card "says it in plain words ('digital gold you hold inside Groww — no locker, no shop')" and "never physical-metal complexity" — a concrete zero-knowledge protection so the user does not expect physical metal. The gold EarnItem (MoreWaysViewModel.kt:101-105, 136-140) and its renderer EarnCard (MoreWaysScreen.kt:260-311) show only: title "Gold", price/%, verdict, optional monthly ₹ line, growwNote "In Groww it is called GOLDBEES." (raw ticker), and a "Buy in Groww" button. The mandated "digital gold... no locker, no shop" reassurance 

### 4. [MEDIUM] PARTIAL — D3 / B0.3a / U1.2  _( guide-moreways )_
- **Defect:** The ETF/Index SIP card — D3's recommended path for small money — does not pre-compute the per-user monthly amount ('put ₹X every month') and names no specific fund; it shows only a generic 'Most funds start from ₹500 a month.'
- **Evidence:** MoreWaysScreen.kt:214 ('Most funds start from ₹500 a month.') and 220 (generic 'NIFTY 50 index fund' under See more, no exact fund named).
- **Failure scenario:** A ₹1,000 user (whom B0.3a routes to SIP/ETF) taps this card expecting 'put about ₹X every month for your money' with the fund chosen for them; instead they get a generic floor and must decide the amount and fund themselves — the gold card computes a per-user amount but the more important index-SIP card does not.
- **Suggested fix:** Compute a per-user monthly ₹ amount from stored capital (reuse the gold-card pattern, MoreWaysViewModel.kt:135-140) and render 'put about ₹X every month'; name the specific fund (e.g. UTI Nifty 50) under See more.
- **Verifier (CONFIRMED):** Re-reading MoreWaysScreen.kt and MoreWaysViewModel.kt against LOCKED spec D3 (REQUIREMENTS_FINAL.md:168) confirms the finding. IndexSipCard (MoreWaysScreen.kt:194-226) is a static composable taking only onSpeak — it has no access to the user's capital, so it cannot pre-compute a per-user amount. Its decision text (l.198-199) says "put a fixed amount"; l.214 shows the generic "Most funds start from ₹500 a month."; and the only See-more content (l.220) is the category "NIFTY 50 index fund", not an exact fund. D3 explicitly requires the app to say "which one and how much per month for the user's 

### 5. [MEDIUM] PARTIAL — D4 / U9.2 / I9  _( guide-moreways )_
- **Defect:** US watch-only cards show price only in '$' with no ₹ conversion, violating D4's requirement that US prices be 'shown with ₹ context'.
- **Evidence:** MoreWaysScreen.kt:276-284 (the non-₹ branch formats '$' with western grouping and no ₹ equivalent); spokenLine at MoreWaysScreen.kt:323 speaks 'Price 150 dollars' only.
- **Failure scenario:** A zero-knowledge Indian user sees 'Apple $211.00' and cannot judge what that is in rupees; the watch-only information is not readable in the only currency they understand.
- **Suggested fix:** Add an approximate ₹ conversion (free Yahoo forex rate) beside the $ price and in the spoken line, e.g. 'about ₹17,600'.
- **Verifier (CONFIRMED):** D4 (REQUIREMENTS_FINAL.md:170) lists "prices shown with ₹ context" as a general attribute of the US-Stocks feature, independent of the BUY-vs-WATCH split. The WATCH-only fallback in the same sentence only downgrades the verdict and requires the honest "special account" line — it never cancels the "₹ context" clause. In code, MoreWaysScreen.kt:276-284 renders US ($) prices via String.format("%,.2f") as e.g. "$211.00" with no ₹ equivalent anywhere on the card; the verdict text (MoreWaysViewModel.kt:170-179) and growwNote (line 121) carry no ₹, monthlyLine is gold-only, and spokenLine (MoreWaysSc

### 6. [MEDIUM] FAIL — H5 / A5  _( honesty-moneymath )_
- **Defect:** H5's resistance-respect ('when a nearby resistance from 1-year data sits before an ATR target, the target respects that level') is not implemented anywhere; targets are pure ATR/stddev multiples with no upper cap, so the app prints unrealistically far targets and over-states expected profit.
- **Evidence:** SignalEngine.kt:231-243 sets t1/t2/t3 = price + atr*{2.0,3.5,5.0} with no resistance clamp; delivery targets in IntradayRepository.widenForDelivery (kt:779-793) are entry + unit*{3.0,5.25,7.5} with no clamp; a repo-wide grep for resistance/swing-high capping returns nothing. Concrete evidence in screenshot 27 (Suggestion record - Stock): OFSS suggested Rs 10,597, Target Rs 14,971 (+41%), stop Rs 8,410 (-21%) — a multi-day delivery T1 sitting 41% away.
- **Failure scenario:** With a Rs 50,000 budget the OFSS delivery pick anchors expectedProfit on a T1 that is +41% away (net profit near Rs 17,000 shown), a level far beyond any recent swing-high that realistically will not be reached in a held-for-days trade — the app over-promises a profit it cannot stand behind (A5), the exact 'formula wishes' outcome H5 was written to prevent.
- **Suggested fix:** After computing ATR/stddev targets, detect the nearest recent swing-high (BUY) / swing-low (SELL) from historicalCloses and clamp any target that lies beyond it to that level; recompute expectedProfit / netProfit and riskReward from the clamped T1 so the shown profit reflects where sellers actually sit.
- **Verifier (CONFIRMED):** H5 mandates a resistance-respect on targets ("When a nearby resistance (recent swing high from 1-year data) sits before an ATR target, the target respects that level — price stops where sellers sit, not where a formula wishes"). Neither level-generation path implements it. Intraday: SignalEngine.kt:231-243 sets t1/t2/t3 = q.price + atr×{2.0,3.5,5.0} with no upper cap. Delivery: IntradayRepository.kt widenForDelivery (kt:775-793) sets targets = entry + unit×{3.0,5.25,7.5} (constants DELIVERY_T1/T2/T3_MULT at kt:121-123) with no cap. A repo-wide grep (resist|swing|high52|clamp/coerce-target) con

### 7. [MEDIUM] FAIL — U7.2 / C10 / B4  _( intraday-tab )_
- **Defect:** The intraday stock-search buy card reuses the TOP pick's RiskGuard result instead of re-evaluating the searched stock's own risk, so B4 auto-blocking (moved >5%, gap >3%, low volume, wide stop-loss) never applies to a searched stock.
- **Evidence:** TradingScreen.kt:320-328 computes riskResult ONLY from topSignal; buyBlockReason derives from it (334-360); the search result card passes that same blockReason=buyBlockReason to MasterSignalCard(signal=found) at 673.
- **Failure scenario:** Market open, capital >=10,000, no per-stock risk on the top pick (or no top pick at all). User searches a stock that has already jumped +7% today; the card renders a BUY with an ENABLED 'INTRADAY order' Groww button because the gap/over-extension guard was computed for a different stock — the app hands a zero-knowledge user exactly the over-extended chase B4/C12a exist to block.
- **Suggested fix:** Re-run RiskGuard.evaluate() with the searched signal's own changePercent/volume/avgVolume/stopLossPct/gapPercent (and vix) and build a per-card block reason for the search result, instead of passing the topSignal-derived buyBlockReason.
- **Verifier (CONFIRMED):** Re-reading the code confirms the mechanism. RiskGuard.evaluate is called in exactly one place (TradingScreen.kt:320) with every per-stock input taken from topSignal (changePercent/volume/avgVolume/stopLossPct/gapPercent, lines 322-327). buyBlockReason derives from that single riskResult (line 347-348), and the search-result card passes blockReason = buyBlockReason at line 673 into MasterSignalCard(signal = found). MasterSignalCard gates its Groww INTRADAY button on netBlock = blockReason ?: stale ?: netProfit≤0 (1764-1810) and performs NO per-signal B4 re-evaluation; its only per-signal guard 

### 8. [MEDIUM] PARTIAL — U7.1 / B0.3a  _( intraday-tab )_
- **Defect:** For small money the risky trade card leads and even shows an enticing green 'Profit after charges' figure while the app is trying to steer the user away; the 'intraday is risky, use Stock/SIP' caution is placed AFTER the decision.
- **Evidence:** Master card renders first (TradingScreen.kt:574-591); the green profit box at 1770-1780 is gated only on netProfit>0 && !stale — it does NOT check blockReason, so it displays even when the Groww button is disabled for a 2,000-9,999 user; IntradayCapitalCaution ('Intraday is risky for ₹X') only appears far below at 629-631, after the ceiling line and weekly score.
- **Failure scenario:** A ₹5,000 user opens Intraday on a signal day: they first read 'BUY <stock> ... N shares ... Profit after charges: about +₹X', an attractive figure, and only after scrolling past the weekly-score line reach the muted 'delivery or SIP is safer' caution — the opposite of U7.1 'the FIRST thing it does with small money is protect me from intraday'.
- **Suggested fix:** For capital <10,000, suppress the green profit figure and the full buy-detail rows (or move the steer-to-safer-path caution above the master card), so protection — not a profit teaser — is the first thing a small-money user sees.
- **Verifier (CONFIRMED):** All three factual claims verified in TradingScreen.kt. (1) The MasterSignalCard (574-591) is the first substantive decision for a ₹2,000-9,999 user — CapitalNotSetCard (530-532) only renders when dailyCapital<=0, and no block card renders on a normal open day, so no small-money protection precedes the card. (2) The green "Profit after charges (estimate): about +₹X" box is gated ONLY by `if (netProfit > 0.0 && !stale)` at line 1770 — it does NOT check blockReason, while the sibling netBlock logic (1764-1769) DOES check blockReason to disable the Groww button (1806, reason "Intraday needs ₹10,00

### 9. [MEDIUM] FAIL — U9.2 / I9  _( jargon-format )_
- **Defect:** The Search-any-stock verdict card (C7) formats every rupee amount with String.format("%.2f"/"%.0f"), bypassing the shared formatIndianRupees(), so no ₹ value on that screen gets Indian grouping.
- **Evidence:** StockResearchScreen.kt:299 (price), 589-590 ("That costs about ₹...of your ₹..."), 614 (stop-loss), also 332/335/401/406/422/424/439/440/783 — all raw String.format, cur=₹ for INR (line 286).
- **Failure scenario:** A beginner opens "Search any stock", searches a high-priced share (e.g. MRF ≈ ₹1,30,000) with ₹1,00,000 capital: the card reads "₹130000.00" and "That costs about ₹... of your ₹100000" — un-grouped, the exact 100,000-vs-1,00,000 bug the shared formatter was built to kill, on a live buy/stop/target decision card.
- **Suggested fix:** Route every ₹ amount on StockResearchScreen (price, day high/low, 52w range, MAs, totalCost, capital, stop-loss) through formatIndianRupees() instead of raw String.format, exactly as HomeScreen/TradingScreen already do.
- **Verifier (CONFIRMED):** Re-read StockResearchScreen.kt in full plus the LOCKED spec. I9 (REQUIREMENTS_FINAL.md:281) and U9.2 (USER_EXPECTATIONS.md:58) mandate "Every ₹ amount uses Indian digit grouping (₹1,00,000, never ₹100,000)". On the live "Search any stock" verdict card (C7, reachable via HomeScreen SearchOverlay → navController.navigate("research/..."), wired in AppNavigation.kt:75-76), cur resolves to "₹" for INR (line 286/384/467) and EVERY rupee value is formatted with raw String.format, never the shared formatIndianRupees(): price (line 299, "%.2f"), the buy-cost sentence "That costs about ₹… of your ₹…" (l

### 10. [MEDIUM] FAIL — U9.1 / I2 (also D3)  _( jargon-format )_
- **Defect:** The Mutual Fund cards show the raw fund-category as a chip with no translation, putting banned "Index Fund"/fund-category jargon on the beginner's main MF card.
- **Evidence:** MutualFundsScreen.kt:1063-1074 (MfCategoryChip renders fund.category verbatim); rendered as the "Index Funds" chip on the "Start here — the simplest fund" card in 16_mf_top.png; the same card's headline is the raw "UTI Nifty 50 Index Fund - Growth Option- Direct" (line 874).
- **Failure scenario:** A zero-knowledge user on the Mutual Fund tab sees a grey "Index Funds" chip (and "Growth Option- Direct" in the title) — I2 explicitly bans "ETF/index fund on cards (say 'ready-made basket of top companies')"; other funds' chips ("Large Cap", "Flexi Cap", "ELSS") are equally untranslated jargon.
- **Suggested fix:** Map fund.category to plain shopkeeper words (or drop the chip on beginner picks) and keep the technical fund name / "index fund" wording behind "See more" per D3; the card face already carries the good plain line "owns all 50 biggest".
- **Verifier (CONFIRMED):** Verified against MutualFundsScreen.kt and 16_mf_top.png. MfCategoryChip (MutualFundsScreen.kt:1063-1074) renders fund.category with no translation (only substringAfterLast(" - ")); category is the raw AMFI scheme_category (MutualFundRepository.kt:410), producing the grey "Index Funds" chip seen in 16_mf_top.png. The card title (line 874) renders fund.name verbatim: "UTI Nifty 50 Index Fund - Growth Option- Direct". I2 explicitly and globally lists "ETF/index fund on cards (say 'ready-made basket of top companies')" in its NEVER list, and states every card must pass ALL checks; U9.1 requires si

### 11. [MEDIUM] PARTIAL — U9.1 / I2, D3  _( mutual-fund )_
- **Defect:** The card surfaces the banned jargon phrase 'Index Funds' as a prominent category chip, which I2 says must be translated ('ready-made basket of top companies') and D3 says must live under See more/Advanced.
- **Evidence:** screenshot 17_mf_scroll.png (grey chip reads 'Index Funds' directly under the fund name); MutualFundsScreen.kt:1063-1075 MfCategoryChip renders category.substringAfterLast(' - ')
- **Failure scenario:** A zero-knowledge user opens the Mutual Fund tab and sees 'Index Funds' as a category label on the #1 recommended card; I2 explicitly lists 'ETF/index fund on cards' as forbidden jargon a village shopkeeper does not know. The 'Why' line translates the concept, but the literal jargon term still appears as an app-authored chip.
- **Suggested fix:** Replace the raw category chip text for index/ETF categories with a plain-words label (e.g. 'Basket of India's 50 biggest' or 'Ready-made basket'); keep the literal 'index fund' wording only inside the expanded 'See more' detail, per D3.
- **Verifier (CONFIRMED):** Verified in source and screenshot. MutualFundsScreen.kt line 895 calls MfCategoryChip(fund.category) inside MfFundCard's always-visible second row (lines 889-908), OUTSIDE the expanded/See-more AnimatedVisibility block that starts at line 993 — so the chip shows on the default collapsed card, including the highlight=true "Start here" #1 beginner card. Line 1064 (category.substringAfterLast(" - ")) yields the literal label "Index Funds". Screenshot 17_mf_scroll.png confirms a grey chip reading "Index Funds" sits directly under the fund name on the top recommended card, in the untapped default v

### 12. [MEDIUM] FAIL — B0.3c / C15a / U4.3  _( protection-chain )_
- **Defect:** The stop-loss 'the app never gives up' escalation is hard-capped at 3 buzzes per breach and then goes completely silent while the position stays open and the loss keeps growing.
- **Evidence:** app/src/main/java/com/example/myapplication3/tracking/TradeWatchService.kt:135-140 (fires only while fired < MAX_REPEAT_ALERTS), :156 (counter cleared ONLY on price recovery, never on user-close), :239 (MAX_REPEAT_ALERTS = 3); grep for 'escalat'/'grown from'/B0.3c across all .kt returns no other implementation.
- **Failure scenario:** User buys an intraday position at 11:00; price hits the stop-loss at 11:20. The watcher buzzes + speaks 3 times over ~3 minutes (once per 90s Yahoo cycle) and then goes silent even though the trade is still open and the loss is deepening. The user was in a meeting and missed those 3 buzzes. B0.3c promises 'strong vibration + voice every few minutes, with live numbers — your loss has grown from ₹300 to ₹450 — it stops only when the user marks the position closed', but from 11:23 the only remaining signal is a no-voice notification roughly every 15 minutes from ScannerWorker (ScannerWorker.kt:86-91), which is neither 'every few minutes' nor voiced and never shows the growing-loss comparison. The core loss-protection promise is broken exactly when it matters most.
- **Suggested fix:** Split the stop-loss (danger) path from the target (profit) path: keep the 2-3x cap only for target hits, but for a stop-loss that remains breached keep re-buzzing AND re-speaking on a throttled 'few-minutes' cadence until the trade is marked closed (clear/stop the escalation on close, not merely on price recovery), and include the growing-loss text ('loss grown from ₹X to ₹Y') the spec quotes.
- **Verifier (CONFIRMED):** The core technical evidence is all accurate. TradeWatchService.kt:135 (`if (fired < MAX_REPEAT_ALERTS)`) + :239 (`MAX_REPEAT_ALERTS = 3`) cap the voiced stop-loss buzz at 3 fires. TradeWatchService.kt:156 (`if (!stopHit) stopAlertCount.remove(t.id)`) resets the counter ONLY on price recovery; line 87's retainAll(openIds) only prunes counters for trades that are already closed, so a still-open, continuously-breached (monotonically-falling) position — exactly the B0.3c scenario — never re-arms: after 3 cycles (~3 min on the 90s Yahoo path, ~45s on the live feed) the foreground service's voice+bu

### 13. [MEDIUM] PARTIAL — B0.2d  _( protection-chain )_
- **Defect:** Brand-specific autostart guidance is implemented only for Realme/Oppo; Xiaomi/Vivo/Samsung — all explicitly named by B0.2d, and MIUI/Funtouch are the most aggressive app-killers — get only the generic battery-optimization exemption, no autostart step.
- **Evidence:** app/src/main/java/com/example/myapplication3/ui/screen/HomeScreen.kt:1524-1527 branches the extra autostart text only on ('realme' in maker || 'oppo' in maker); a repo-wide grep for xiaomi/miui/vivo autostart handling finds none in any user-facing flow.
- **Failure scenario:** A Xiaomi (MIUI) user taps the battery card and grants 'ignore battery optimizations', but MIUI's separate Autostart toggle in the Security app stays OFF. MIUI then freezes TradeWatchService and ScannerWorker in the background, so the stop-loss alert never arrives — the precise silent-kill B0.2d exists to prevent — and nothing in the app ever told this user to flip Autostart, because the brand guidance only triggers for Realme/Oppo.
- **Suggested fix:** Detect Build.MANUFACTURER for xiaomi/redmi/poco, vivo/iqoo, oneplus and samsung and show each brand's exact Autostart / lock-in-Recent-apps steps in plain English, the same way Realme/Oppo already do.
- **Verifier (CONFIRMED):** Spec B0.2d (REQUIREMENTS_FINAL.md:47) explicitly names Xiaomi/Vivo/Oppo/Realme/Samsung and mandates "that brand's exact steps" for background running + battery No-restriction + autostart, calling it "critical on Indian phones." In code, the only Build.MANUFACTURER branch is HomeScreen.kt:1524-1532, which shows the extra autostart/lock-in-Recents guidance ONLY when 'realme' or 'oppo' is in the maker string. All other brands get the generic line (1516) whose tap fires BatteryGuard.requestIntent (BatteryGuard.kt:51-67) — strictly the Android ignore-battery-optimizations exemption, which does NOT 

### 14. [MEDIUM] PARTIAL — B2 / U1.6 / U5.3  _( realdata-freshness )_
- **Defect:** The Intraday freshness line (and the voice-refuse guard) key staleness solely on the isUsingCachedData boolean with no wall-clock check, so old prices can briefly appear green/'fresh' and the voice can speak buy advice on them.
- **Evidence:** TradingScreen.kt:886,899 (dot/label chosen only by `isCached`, else green 'Updated $timeText'); InterdayViewModel.kt:926 sets isUsingCachedData=false on every success and :1013 the auto-refresh loop suspends while backgrounded; voice guard at InterdayViewModel.kt:1313-1316 also keys only on isUsingCachedData. Contrast HomeScreen.kt:696-697 (Stock header adds isOld = now-fetchedAtMs > INDEX_STALE_AFTER_MS = 5 min, HomeScreen.kt:59).
- **Failure scenario:** User backgrounds the Intraday tab for ~15 minutes and returns; the last fetch succeeded 15 min ago so isUsingCachedData is still false. Until the resumed fetch completes, FreshnessLine shows a green dot 'Updated 12:46 pm' (no amber, no relative age) over 15-minute-old prices, and if the user taps the speaker in that window the voice reads out actionable buy prices on stale data — the exact harm B2 guards against. On the Stock tab the same data would already read amber 'Cached 12:46'.
- **Suggested fix:** Give the Intraday FreshnessLine (and the voice-refuse guard) the same wall-clock staleness fallback the Stock header uses: treat data as stale when now - cacheTimestamp exceeds a small threshold (e.g. 3-5 min) regardless of isUsingCachedData, driving both the amber label and the voice 'prices are old' refusal.
- **Verifier (CONFIRMED):** All cited lines re-verified. TradingScreen is the Intraday tab (AppNavigation.kt:65,14). FreshnessLine (TradingScreen.kt:886,899-901) keys dot-color and label SOLELY on isCached, and is fed only isCached=state.isUsingCachedData plus a formatted string (440-442) — no epoch is passed, so no wall-clock age check is possible. InterdayViewModel sets isUsingCachedData=false on every success (:926), true only on failure (:975), and the auto-refresh loop suspends while backgrounded via _isForeground.first{it} (:1013), so after a ~15-min background gap the last success is 15 min old yet isUsingCachedDa

### 15. [LOW] PARTIAL — C24 / B10  _( guide-moreways )_
- **Defect:** The Guide 'Advanced' area hosts only the More Ways entry and the glossary; the H13 'How was this number made?' score breakdown and the E1a detailed report card — which C24/B10 explicitly name as Advanced-area residents — are not surfaced here.
- **Evidence:** LearningScreen.kt:145-237 (the Advanced section contains exactly the 'More ways to earn' card and the collapsed glossary — nothing else).
- **Failure scenario:** A curious/growing user opens Guide > Advanced expecting the spec's stated 'home for' the score breakdown and the detailed weekly report card and finds neither. (They may exist on the pick card / Stock tab, but C24/B10 route them through the Guide Advanced area.)
- **Suggested fix:** Surface the E1a report card and an entry to the H13 score breakdown inside the Guide Advanced area (or amend the locked C24/B10 wording if on-card placement is intended).
- **Verifier (CONFIRMED):** C24 (REQUIREMENTS_FINAL.md:157) and B10 (line 115) both explicitly, by rule number, designate the Guide "Advanced" sub-section as the home for the H13 "How was this number made?" score breakdown and the E1a detailed report card ("live here" / "the home for ..."). The code's Advanced section (LearningScreen.kt:145-237) contains only two residents: the "More ways to earn" nav card (162-200) and the collapsed glossary (202-286). Neither the score breakdown nor the report card is there. Independent grep of the whole source tree confirms: the E1a report card is rendered only on the Home/Stock tab (

### 16. [LOW] PARTIAL — H5 / B0.3a  _( honesty-moneymath )_
- **Defect:** The round-trip charge model is purely proportional with no flat-brokerage floor, so for the small positions where intraday first unlocks it under-deducts versus the spec's '~Rs 50' and real charges, over-stating net profit in the unsafe direction.
- **Evidence:** TradingSignal.kt:87-89 (INTRADAY_COST_RATE 0.0012 + SLIPPAGE 0.0005, no fixed component) and roundTripCostRs (kt:97-101) scale only with position value; for a ~Rs 10,000 intraday position (capitalPathFor FULL starts at Rs 10,000, TradingSignal.kt:168-172) this deducts ~Rs 17, below the spec's '~Rs 50' (H5) and below real per-round-trip charges of ~Rs 25-30.
- **Failure scenario:** A user at the Rs 10,000 threshold is shown a net-profit figure that omits ~Rs 10-15 of real charges; on a thin-edge intraday setup the shown green net profit can exceed the true after-charge profit — contradicting the code's own comment that 'under-promising profit is the safe error to make'.
- **Suggested fix:** Add a small flat per-order floor to roundTripCostRs (e.g. max(2*flatBrokerage + proportional, ...)) so tiny positions carry a realistic minimum charge, keeping the deduction on the conservative side of the true cost.
- **Verifier (CONFIRMED):** The cost model is purely proportional with no flat-brokerage floor: roundTripCostRs = tradeValue × (INTRADAY_COST_RATE 0.0012 + SLIPPAGE_BUFFER_RATE 0.0005) (TradingSignal.kt:87-101). Intraday first unlocks at capital >= Rs 10,000 (capitalPathFor FULL, TradingSignal.kt:168-172), and at a ~Rs 10,000 position this deducts only 10,000 × 0.0017 = Rs 17. The LOCKED spec requires more: H5 (REQUIREMENTS_FINAL.md:237) 'minus ~Rs 50 charges', line 84 '~Rs 50 charges already deducted', B0.3a (line 56) '~Rs 50-60 per round trip', and H11 (line 249) 'AFTER deducting realistic charges'. Real Groww intraday

### 17. [LOW] PARTIAL — H13 / C24  _( honesty-moneymath )_
- **Defect:** H13's 'How was this number made?' per-factor arithmetic breakdown (Trend +15, Volume +13, Momentum +7 ... = 91) is not implemented; only the summary confidenceMeaning line is shown.
- **Evidence:** Repo-wide grep for 'How was this number' / 'score breakdown' returns no matches; the card shows only confidenceMeaning 'score X out of 100 (how many good signs lined up)' (TradingScreen.kt:1827), and SignalEngine discards the per-factor point contributions after summing (SignalEngine.kt:101-211).
- **Failure scenario:** A curious/growing user taps for the transparency H13/C24 promise (the exact arithmetic behind the score) and finds only the adjective + total, so trust cannot be built 'on arithmetic, not adjectives' as the spec requires.
- **Suggested fix:** Carry the per-factor point contributions on the signal (or recompute them) and render the itemised list under a 'How was this number made?' tap in the Advanced/See-more area, per H13/C24.
- **Verifier (CONFIRMED):** Spec H13 (REQUIREMENTS_FINAL.md:253) explicitly requires a "How was this number made?" tap that opens the EXACT per-factor arithmetic — "Trend +15, Volume +13, Momentum +7, News +3, Risk −2 = 91 — trust from arithmetic, not adjectives"; C24 (line 157) also requires it as an entry point in the Guide Advanced area. The code does not do this. SignalEngine.kt:101-211 computes per-factor bullPoints/bearPoints (+15,+12,+7…) but at line 305/332 stores only the plain-English adjective reason strings; the point contributions are discarded, leaving only the summed confidence total on TradingSignal. The 

### 18. [LOW] PARTIAL — U0 / I8 / C10  _( intraday-tab )_
- **Defect:** On a no-signal day the green 'Safety Check: Safe to Trade' checkmark is shown directly under the amber 'NO TRADE' card, a mixed message for a zero-knowledge user.
- **Evidence:** Screenshot 14 shows 'NO TRADE' (amber) immediately above 'Safety Check — Safe to Trade' (green tick) + 'Market mood calm'; when topSignal is null, RiskGuard.evaluate is fed zeros and returns 'All clear' (TradingScreen.kt:316-328; RiskGuard.kt:174-179,1064).
- **Failure scenario:** A first-time user reads a green 'Safe to Trade' tick next to a bold amber 'NO TRADE' and cannot tell in 10 seconds whether they should trade or not — failing the U0/I8 ten-second clarity test.
- **Suggested fix:** When there is no qualifying signal, relabel the safety card to reflect the outcome (e.g. 'Conditions calm, but no good trade today') rather than a green 'Safe to Trade' that contradicts the NO-TRADE verdict directly above it.
- **Verifier (CONFIRMED):** Verified the exact code path and screenshot. TradingScreen.kt:316 sets topSignal=state.signals.firstOrNull(); when null, RiskGuard.evaluate (320-328) gets all-zero inputs. In RiskGuard.kt:143-179 those zeros produce an empty warnings list (Rule 4 skipped because avgVolume>0L is false; Rules 3/5/6/7 don't fire at calm VIX), so primaryMessage='All clear, you can trade', isBlocked=false, canTrade=true. TradingScreen.kt:594-604 then renders NoTradeCard (signals empty) and, because !riskResult.isBlocked, SafetyCheckCard directly below it; 1060-1064 pick the green CheckCircle 'Safe to Trade' and 108

### 19. [LOW] PARTIAL — B4  _( intraday-tab )_
- **Defect:** Two of the five risky conditions B4 names — very low volume and stop-loss too wide — are MEDIUM severity and never count toward the '3+ warnings = full block', so they can never contribute to an automatic block.
- **Evidence:** RiskGuard.kt blocks only when highCount>=3 (167-171); low-volume is RiskSeverity.MEDIUM (145-147) and wide stop-loss is MEDIUM (156-158), while gap/moved/VIX/near-close are HIGH.
- **Failure scenario:** A stock with very low volume + a >3% stop-loss + one HIGH condition presents three of B4's listed risky conditions, yet only one HIGH is counted, so no full block fires and the buy stays enabled — arguably short of 'full block when 3+ serious warnings combine'.
- **Suggested fix:** Either count MEDIUM warnings toward the stack (e.g. block at highCount>=3 OR total serious count where 2 MEDIUM = 1 HIGH), or document explicitly that only HIGH-severity conditions are 'serious' per B4.
- **Verifier (CONFIRMED):** Verified in RiskGuard.kt: low-volume (line 146) and wide stop-loss (line 157) are RiskSeverity.MEDIUM; the block at lines 168-171 counts only HIGH severity, so two of B4's five named risky conditions can never contribute to the automatic block. Reachable via SignalEngine (line 74, no SL/volume filter) and TradingScreen (lines 320-325), so a card with low-volume plus wide-SL plus one HIGH shows three of B4's conditions yet stays buyable. B4 mandates a full block when 3-plus serious warnings combine; the code falls short of the literal locked spec. Low severity because warnings still display and

### 20. [LOW] PARTIAL — U9.2 / I9  _( jargon-format )_
- **Defect:** The Settings "My money" amount is shown with no Indian grouping.
- **Evidence:** SettingsScreen.kt:703-705 (fmtFieldValue uses Locale.US "%.0f") seeds the money field at line 727; screenshot legend 20_settings_top records "Total Money 100000".
- **Failure scenario:** A user who set ₹1,00,000 reads back "100000" in Settings instead of "1,00,000". It is an editable input (commas complicate typing), so this is softer than a display, but it is still a ₹ amount the user reads without Indian grouping.
- **Suggested fix:** Show the saved amount grouped via formatIndianRupees in a read-back label (keep the editable field itself raw for parsing), so the confirmed money reads "1,00,000".
- **Verifier (CONFIRMED):** The finding is factually accurate and the spec requirement is absolute. SettingsScreen.kt:143-150 renders the "My money" capital field as NumberTextField(label="Total Money You Have (₹)", value=state.capital). NumberTextField seeds its text via fmtFieldValue(value) (line 727), and fmtFieldValue (lines 703-705) formats whole numbers with String.format(Locale.US, "%.0f", v) — plain "100000", no digit grouping. This is a ₹ amount (the label literally says "(₹)"), and because the field re-seeds from the SAVED value on screen entry (per the NumberTextField doc comment, lines 707-714), a returning u

### 21. [LOW] PARTIAL — U9.2 / I9  _( jargon-format )_
- **Defect:** The Watchlist (behind "See more") formats INR prices with Locale("en","IN") "%,.2f", which on Android groups in threes, not lakh/crore.
- **Evidence:** WatchlistScreen.kt:41-44 (fmtPrice: INR → String.format(localeIN, "%,.2f", v)) — the very pattern CommonComponents.kt:45-47 documents as producing "100,000" instead of "1,00,000".
- **Failure scenario:** A watchlisted share priced above ₹1 lakh (e.g. MRF, Page Industries) renders "1,00,000.00" grouped Western-style rather than Indian; inconsistent with every other tab. Edge-only (needs a ≥₹1 lakh price and the See-more path).
- **Suggested fix:** Replace the INR branch with formatIndianRupees(v, 2), matching HomeScreen/TradingScreen/MoreWays.
- **Verifier (CONFIRMED):** WatchlistScreen.kt:41-44 defines fmtPrice for INR as String.format(Locale("en","IN"), "%,.2f", v) and applies it to each row's price at line 311. CommonComponents.kt:45-47 — the codebase's own authoritative comment — states this exact pattern "does NOT produce lakh/crore grouping on Android — java.util.Formatter always groups in threes, so users saw 100,000 instead of the 1,00,000"; the %,.2f uses the identical ',' grouping flag with the same localeIN, so it behaves the same. The LOCKED spec is explicit: U9.2 "Every ₹ number uses Indian grouping" and I9 "Every ₹ amount uses Indian digit groupi

### 22. [LOW] FAIL — U9.2 / I9  _( mutual-fund )_
- **Defect:** The monthly SIP-reminder notification prints the rupee amount without Indian digit grouping, violating the I9/U9.2 grouping rule.
- **Evidence:** SipReminderWorker.kt:51 and :53 — "Did your ₹${record.monthlyAmount} get taken?" uses the raw Int, not formatIndianRupees
- **Failure scenario:** A user tracking a ₹2,000 or ₹10,000 SIP gets a phone notification reading 'Did your ₹2000 get taken?' / '₹10000' — ungrouped digits, whereas every in-app ₹ value uses formatIndianRupees (e.g. MySipCard line 772 shows '₹2,000'). Inconsistent and against U9.2.
- **Suggested fix:** Format the amount with the shared Indian-grouping formatter in the notification title/text (e.g. "₹${formatIndianRupees(record.monthlyAmount.toDouble())}").
- **Verifier (CONFIRMED):** Re-read SipReminderWorker.kt:51 and :53-54: the monthly SIP notification builds its text as "Did your ₹${record.monthlyAmount} get taken?". SipStore.kt:28 shows monthlyAmount is an Int, so this is raw string interpolation with no digit grouping. formatIndianRupees (CommonComponents.kt:70; groupIndianDigits at :107) groups at the thousands level, not only the lakh level ("1234"→"1,234", "100000"→"1,00,000"), so a SIP of ₹1,000/₹2,000/₹10,000 renders ungrouped as "₹1000"/"₹2000"/"₹10000" instead of "₹1,000"/"₹2,000"/"₹10,000" — a genuine visible difference for any amount ≥₹1,000 (typical SIP amo

### 23. [LOW] PARTIAL — U6.2 / B6, C18  _( mutual-fund )_
- **Defect:** The collapsed 'Start here' projection floors a losing fund's assumed growth to +8% and shows a rosy future value with NO loss warning; the 'this fund lost money' caution exists only inside the expanded calculator.
- **Evidence:** MutualFundsScreen.kt:981-988 (collapsed line calls projectSipValue) and projectSipValue:1159-1166 (return3y < 0 → annualPct = 8.0); the fundHasLost red warning lives only in the expanded MfSipCalculator (lines 1270-1280)
- **Failure scenario:** In a 3-year bear window an index/large-cap curated fund's 3Y CAGR goes negative. The default (un-expanded) #1 card still shows 'invest ₹1,000/month, in 5 years it could become about ₹X — an estimate' computed at 8% growth, beside a red declining sparkline and a negative 1Y, but with no 'this fund has been losing' caution unless the user expands the card — a subtle B6 honesty gap for the exact user who never taps deeper.
- **Suggested fix:** In the collapsed highlight block, when return3y < 0 replace the growth projection with the same honest 'Careful: this fund lost about N% a year over 3 years' line used in the expanded calculator, or suppress the projection entirely.
- **Verifier (CONFIRMED):** Every cited code fact checks out. MutualFundsScreen.kt:979-988 renders the collapsed "Start here" projection ("...in 5 years it could become about ₹X — an estimate, not a promise") only when highlight && !expanded, computed via projectSipValue. projectSipValue (1159-1166) floors a losing fund (return3y < 0.0) to annualPct = 8.0 — a positive ~₹73,960-on-₹60,000 growth story. The fundHasLost red caution "Careful: this fund LOST about X% a year" (1270-1280) lives only inside MfSipCalculator, which renders solely under AnimatedVisibility(visible = expanded) (line 993), so a non-expanding user neve

### 24. [LOW] PARTIAL — U6.1  _( mutual-fund )_
- **Defect:** The tab's leading sentence sells growth ('It grows big') rather than stating in one sentence WHY SIP is the SAFE way, as U6.1 requires.
- **Evidence:** MutualFundsScreen.kt:221-227 tagline 'Save a little every month. It grows big.'; the 'What is a mutual fund?' banner (445-450) also omits the safety rationale (screenshot 16)
- **Failure scenario:** A user opening the safe-path tab is told SIP 'grows big' but is never told, in one plain sentence, why drip-feeding small amounts is the low-risk way (averaging out ups and downs). The safety idea is only implied later in the 'never stop when the market falls' line, not stated up front as U6.1 asks.
- **Suggested fix:** Lead with a one-line safety rationale, e.g. 'Adding a little every month spreads out the risk — the safest way to grow money slowly,' then the one-fund answer.
- **Verifier (CONFIRMED):** U6.1 requires the tab to "tell me in one sentence why SIP is the safe way, then give me ONE simplest fund first." The leading sentence (MutualFundsScreen.kt:221-227) is "Save a little every month. It grows big." — its emphasized clause pitches GROWTH, not the safety rationale. The "What is a mutual fund?" banner (445-450: "Experts invest your money in shares. With a SIP, you add a little every month.") only states the SIP mechanism, not WHY drip-feeding is low-risk (averaging across price levels). The averaging idea appears only later and only as a "never stop when the market falls" discipline

### 25. [LOW] PARTIAL — B2 / U1.6  _( realdata-freshness )_
- **Defect:** After a failed live refresh the app stops using the plain-words 'from X minutes ago' age and reverts to a raw clock time / vague 'from earlier', reintroducing the clock-math burden its own helper exists to avoid.
- **Evidence:** InterdayViewModel.kt onFailure branch (968-997) sets isUsingCachedData=true but never re-sets lastUpdated, leaving the previous success's 'hh:mm a' string; TradingScreen.kt:882-883 then renders 'Old price - from 12:46 pm' (a clock time), and the screen-level chip at TradingScreen.kt:377-379 renders the vaguer 'Old price - from earlier'. The cold-open path uses staleAgeLabel(cacheTs) for precise minutes (InterdayViewModel.kt:795), and staleAgeLabel's own doc (1208-1210) says a raw timestamp 'forces a zero-knowledge user to do clock math' and is wrong.
- **Failure scenario:** During a live session Yahoo starts failing; the amber stale line reads 'Old price - from 12:46 pm' at 1:05pm, forcing the beginner to compute the ~19-minute age themselves — the precise 'from 19 minutes ago' string is available from repository.getCacheTimestampMs() but is not recomputed on the failure path.
- **Suggested fix:** In the onFailure branch set lastUpdated = staleAgeLabel(repository.getCacheTimestampMs()) so every stale state (cold-open AND failed refresh) shows the same plain-words 'from X minutes ago' age.
- **Verifier (CONFIRMED):** Every cited point verifies. InterdayViewModel.kt onFailure (968-997) sets isUsingCachedData=usingCache and lastFetchFailed=true but never assigns lastUpdated; a grep confirms lastUpdated is set in only two spots — line 795 (staleAgeLabel, cold-open cache) and line 922 (SimpleDateFormat("hh:mm a"), onSuccess). So after a successful live fetch followed by a failure, lastUpdated keeps the raw clock string (e.g. "12:46 PM"). TradingScreen FreshnessLine (882-883) wraps any non-"Old price" string as "Old price - from $timeText", yielding "Old price - from 12:46 PM" (a clock time) and renders it ambe

### 26. [LOW] PARTIAL — B2 / U9.5  _( realdata-freshness )_
- **Defect:** The stale-data voice always blames 'No internet', even when the phone has internet and only the price fetch failed, asserting a specific wrong cause.
- **Evidence:** InterdayViewModel.kt:1314 and :1347 speak 'No internet. This is an old price...' / 'No internet. I will not tell you to buy or sell on old prices...' whenever isUsingCachedData is true, but isUsingCachedData is set on ANY fetch failure (onFailure sets usingCache from every error type, InterdayViewModel.kt:960-975), not only network loss.
- **Failure scenario:** Internet is working but Yahoo returns HTTP 401/timeout; the app falls back to cached data and, on a speaker tap, the voice tells the user 'No internet' — an inaccurate cause that could send the user to toggle a connection that is actually fine.
- **Suggested fix:** Speak the cause-neutral line the spec specifies ('Prices are old — wait for fresh prices.') for generic staleness, and reserve the 'No internet' wording for the actual UnknownHost/Connect exception cases the onFailure branch already distinguishes at InterdayViewModel.kt:983-987.
- **Verifier (CONFIRMED):** Re-read InterdayViewModel.kt (ui/viewmodel/ path; the finding's file path dropped the ui/viewmodel/ segment but the line numbers align exactly). Line 1314 speaks "No internet. This is an old price. Do not trust it." and line 1347 speaks "No internet... Please turn on the internet." — both gated ONLY on isUsingCachedData. That flag is set to usingCache = keepSignals.isNotEmpty() at line 961/975 in onFailure, which fires for EVERY error type, not just network loss. The claim is confirmed and understated: the same onFailure block (lines 980-995) branches the VISUAL banner into "No internet" vs "T

### 27. [LOW] PARTIAL — I9 / U9.2  _( settings-voice-keys )_
- **Defect:** The 'Total Money You Have (₹)' value is displayed ungrouped ('100000') instead of Indian grouping ('₹1,00,000') required for every ₹ amount.
- **Evidence:** Screenshot 20 shows '100000'; SettingsScreen.kt:143-150 renders state.capital via fmtFieldValue (SettingsScreen.kt:703-705) with no Indian grouping, while the preset lines correctly use formatIndianRupees (SettingsScreen.kt:329-331).
- **Failure scenario:** A beginner reads '100000' with no grouping — the one ₹ figure on the money screen that violates the Indian-digit rule the rest of the app follows. (Editable input field, so lower impact.)
- **Suggested fix:** Show the money value with Indian grouping when the field is not being actively edited, or format on blur.
- **Verifier (CONFIRMED):** Genuine literal violation of I9/U9.2. SettingsScreen.kt:143-150 renders the "Total Money You Have (₹)" field via NumberTextField, which seeds its text with fmtFieldValue (SettingsScreen.kt:703-705 → String.format(Locale.US, "%.0f", v)), producing "100000" with no grouping. Screenshot 20 confirms "100000" is shown while the Safe/Normal preset money lines directly below (SettingsScreen.kt:321-330, using formatIndianRupees) correctly render "₹2,000", "₹1,000", "₹3,000". The label "(₹)" makes this unambiguously a ₹ amount, and I9 (REQUIREMENTS_FINAL.md:281) states "Every ₹ amount uses Indian digit

### 28. [LOW] PARTIAL — B9 / U1.4  _( stock-tab )_
- **Defect:** The qty<1, afford<1 and profit<=0 decision branches state WHY there is no trade but do not point to what IS possible, unlike the other no-trade branches — a soft dead end.
- **Evidence:** HomeScreen.kt:1974-1980 (qty<1) returns only 'Even 1 share risks more than your safety limit — skip this one today.' with no SIP/next-step line; same for afford<1 (1966-1973) and profit<=0 (1981-1987). Contrast best==null (1925-1936) and allUnaffordable (1913-1923) which do add 'Grow your money safely with a monthly SIP — tap Mutual Fund below.'
- **Failure scenario:** On a live market day the ₹50,000 user's only pick (OFSS) floors to qty 0, so the hero decision card shows 'skip this one today' with no 'what is possible now' pointer — U1.4 ('if there is nothing to do, tell me what IS possible') is only half met.
- **Suggested fix:** Append the same 'Grow your money safely with a monthly SIP — tap Mutual Fund below' pointer (and/or Practice) to the qty<1, afford<1 and profit<=0 branches so every no-trade state ends with a next step (B9).
- **Verifier (CONFIRMED):** Re-read HomeScreen.kt (ui/screen/HomeScreen.kt) 1898-1987 and spec B9 (REQUIREMENTS_FINAL.md:109) + U1.4. The qty<1 branch (1975-1978, "Even 1 share risks more than your safety limit — skip this one today.") and the profit<=0 branch (1982-1985, "Don't buy today — the profit is too small.") state the WHY but include NO positive "what IS possible" pointer — unlike every sibling no-trade branch: best==null (1931-1934, whose own comment reads "Never a dead end (B9): always point to what IS possible right now"), allUnaffordable (1918-1921), and SIP_ONLY (1905-1908), all of which append the SIP line

---
## REFUTED (checked, NOT real — dropped by the skeptic pass)

- **B7 / A5 / U9.6 / B3** (stock-tab): The 'honest' Stock suggestion record logs picks straight from the ungated dailyPicks list, so it records BUY suggestions the Stock tab never surfaced to the use  → REFUTED: The finding conflates the app's PICK-QUALITY scorecard with the user's realized-P&L record, which the spec treats as two different things and the app keeps deliberately separate. E1a defines the report card over "this we
- **B7a / U1.4** (stock-tab): Budget-fit filters picks on 1-share affordability only, ignoring risk-sizeability, so an un-buyable high-priced stock is chosen as THE day's pick and the honest  → REFUTED: The evidence FACTS are accurate (budget filter is price-only at HomeScreen.kt:897; hero/best selection at :901/:1713 ignores sizeability; riskQty floors to 0 for a ₹50k user on OFSS at :1955-1958), but the claimed HARM d
- **B7b / U4.4** (stock-tab): Today's P/L is not visible 'at ALL times' — the top P/L bar is hidden entirely when no budget is set and no trade is open, contradicting the literal always-visi  → REFUTED: Code is as cited (TodayPnlBar.kt:47-48 returns when realizedPnl==0 && openTradeCount==0 && budget<=0; dailyCapital defaults to 0 in InterdayViewModel.kt:449-451; skipCheckInToday only records a skip date). But this is a 
- **C12a / U7.3 / B8** (intraday-tab): On the free-Yahoo path (no Angel One keys, this device) stale-entry protection can silently be a no-op, because the entry zone is recomputed from the current pr  → REFUTED: The finding's mechanism is factually wrong because it omits the per-day entry-zone anchoring layer. While SignalEngine.analyze() does recompute entryLow/entryHigh from q.price each call (SignalEngine.kt:302-303), Interda
- **U7.2 / B0.2a / C15a** (intraday-tab): The master card's default view lacks explicit 'sell today' / 3:15 square-off clarity U7.2 asks for; the only validity wording ('Till 3:15 PM today') is hidden b  → REFUTED: The finding's central evidence is factually wrong. It claims the default decision view (TradingScreen.kt:1700-1817) "has no line telling the user this must be closed today." In fact GrowwActionRow is called at TradingScr
- **E1 / owner-decision-2 / E3d** (protection-chain): The promised ~90s foreground watch cannot be sustained across a full/overnight hold — the dataSync FGS hits Android's ~6h/day cap (and won't auto-restart from b  → REFUTED: The finding's technical observations are accurate (TradeWatchService.kt:68 uses the 6h/day-capped FOREGROUND_SERVICE_TYPE_DATA_SYNC; :92 keeps the FGS alive overnight; onTimeout :177-185 stops it; BootReceiver.kt:44-48 s
- **U6.3 / C20, B0.2** (mutual-fund): The single Groww button opens Groww's SEARCH page for the fund name, not the fund's page directly as C20 specifies.  → REFUTED: C20 (REQUIREMENTS_FINAL.md:149) requires the button to open the fund page "directly (B0.2 pattern)" — it binds itself to the B0.2 pattern, not to an unqualified "directly." B0.2 (line 39) explicitly sanctions the fallbac
- **U6.4 / C20** (mutual-fund): SIP tracking offers only Day 1/5/10 chips, so a user whose real Groww SIP falls on another date cannot align the monthly reminder even though the data model sup  → REFUTED: Spec C20 says verbatim The app picks the date, so restricting to Day 1/5/10 chips is spec-endorsed, not a defect. App guidance (MutualFundsScreen.kt:581,596,1376) recommends 1st or 5th, matching the chips. U6.4 and C20a 
- **U1.7 / I1, I2** (mutual-fund): The card shows bare trailing-return percentages under terse '1Y' / '3Y' labels — numbers with no plain-word meaning attached, which U1.7/I1 forbid.  → REFUTED: The chips are not "numbers with no meaning." MfReturnChip (MutualFundsScreen.kt:1044-1060, color logic 1046-1050) renders -3.3% in red and +7.9% in green, so each carries instant directional meaning via the app's standar
- **U2.3 / I3** (mutual-fund): The expanded fund view is effectively a two-slider SIP calculator (monthly amount AND number of years), which leans into the 'never give the user a calculator /  → REFUTED: The finding's facts are accurate (MutualFundsScreen.kt:1221-1249 = two sliders, ₹500-10,000/month and 1-20 years, feeding the projection), but it is not a spec violation. U6.2 EXPLICITLY mandates this feature: "It answer
- **D4 / U2.1** (guide-moreways): Watch-only US cards still render a 'See in Groww' button that searches an Indian broker for a US company name, which typically returns nothing useful — a dead-e  → REFUTED: D4 (REQUIREMENTS_FINAL.md:170) only bars "a BUY the user cannot act on" and requires WATCH-only + one honest line; it does not forbid a look-up button. The code satisfies this: MoreWaysScreen.kt:302 deliberately labels t
- **A4 amendment / C3 / U1.5** (settings-voice-keys): With 'Speak alerts in Hindi' ON, the Stock tab's main 'Listen' read-aloud (the C3 zero-reading path) still speaks English, so the Hindi voice does not cover the  → REFUTED: The finding's mechanical claim is factually accurate — the Stock tab's big "Listen to today's plan" button (HomeScreen.kt:1085-1101) calls vm.speakDaySummary() (InterdayViewModel.kt:1342-1389), which builds English text 
- **E6 / B0.4 — U9.1, I6** (settings-voice-keys): The Angel One card carries two lines that read as a contradiction with no reconciliation for the user: 'the app can never place, change, or cancel orders' and '  → REFUTED: Re-read SmartApiSettingsCard.kt:78-82 and :176-178 in full context. The two lines are not an unreconciled contradiction: they address different subjects and the card reconciles them. Line 79 (bold) states the APP is read
- **U7.4 / B6** (honesty-moneymath): The intraday master card shows a fabricated probability distribution 'Up X% / Flat Y% / Down Z%' — bare percentages engineered from the checklist score to sum t  → REFUTED: The finding's core harm is factually wrong. The "Up X% / Flat Y% / Down Z%" block (TradingScreen.kt:1867-1882) lives entirely inside `if (showMore)` (opened line 1824), so it is NOT on the default master card — the zero-
- **U9.3 / I9** (jargon-format): Brand/CTA green and gain/up green are the identical hex, so green simultaneously means "gain" and "brand action", softening I9's "green = gain/up only, meanings  → REFUTED: The hex-identity fact is real (Color.kt:19 GoldAccent = Color.kt:25 GreenPrimary = 0xFF00B386, and gain elements at HomeScreen.kt:725/1389 and CommonComponents.kt:484 use that same hex as the brand CTA/header at HomeScre
- **U9.1 / I2** (jargon-format): The Guide search box suggests a banned jargon token as an example before any translation is shown.  → REFUTED: The placeholder at LearningScreen.kt:244 ("Search term... (e.g. RSI, IPO)") lives inside the sanctioned translation surface, not on any decision path. It only renders after the user opts into the collapsed "Glossary — Ma
- **U9.1 / C7 (search: flag confusing foreign listings)** (neverempty-blocking): Search results list foreign listings with no flag warning that they cannot be bought in Groww, using the same brand-green exchange chip as NSE.  → REFUTED: The finding's factual observations are correct — HomeScreen.kt:646-657 renders every exchange chip identically (DarkSurfaceElevated + GoldAccent) and IntradayRepository.kt:992 keeps foreign rows verbatim after Indian .NS
- **B9 / U5.2 / U1.4 (Intraday no-trade next step)** (neverempty-blocking): The Intraday NO-TRADE card states WHY but does not itself point to what IS possible now; the only next-step pointer (Stock tab / SIP) is a separate muted line l  → REFUTED: The finding's factual description is accurate but it is not a locked-spec violation. B9 requires that a no-trade screen "say WHY in one simple line and always point to what IS possible right now... No screen may ever sho

---
## WHAT PASSED (evidence of conformance, by dimension)

### stock-tab — 13 pass-items
- U1.1 / C1 / C2: the tab leads with a plain words market verdict then the one decision — CompactStatusHeader shows 'Market is down today — be extra careful' + NIFTY points (HomeScreen.kt:724-744, screenshot 13_stock_50k_decision_top.png).
- U1.3 / B0.2 / B0.2a: BUY cards end with the Groww action — big 'In Groww, choose: DELIVERY' label + one 'Place order in Groww' deep-link button (GrowwAction.kt:107-153; HomeScreen.kt:2103-2109).
- B0.2b: 'Order placed? Yes/No' + fill-price/qty confirm survives process death via DataStore pending-order marker (InterdayViewModel.kt:341-416; GrowwAction.kt:171-197, 207-275).
- B0.2c: exact per-target share counts, never 'half' — computeTargetSplit + 'Sell plan: 1st target - sell N' (GrowwAction.kt:33-48; HomeScreen.kt:2083-2090).
- B0.3a capital-path: <₹2,000 routed to SIP-only ('would lose most of it to charges'), thresholds 2000/10000 (HomeScreen.kt:1900-1910; TradingSignal.kt:168-171); ₹10k shows budget-fit message (screenshot 09_stock_10k_top.png).
- B7 same-picks: the day's picks are saved and reloaded stable all day, only prices refresh (InterdayViewModel.kt:1277-1307).
- B7b P/L bar + tappable budget chip: 'PRACTICE profit today: +₹0' + 'My budget: ₹50,000 ✎', 48dp edit target (TodayPnlBar.kt:39-151; screenshot 13).
- B7b check-in: asked once per trading day with tap amounts + custom + 'Not today', skipped on weekends/holidays (HomeScreen.kt:1308-1360; InterdayViewModel.kt:457-474; screenshot 07_budget_dialog.png).
- B10 progressive disclosure: one decision + collapsed finder + optional chips, no inner tabs (HomeScreen.kt:993-1303; screenshot 13).
- B3 (when a pick renders): buy price, stop-loss with meaning, target+share split, qty, risk/Beginner-Safe, Strong/Okay/Weak setup word, reasons, profit-after-charges, Groww action all assembled together (HomeScreen.kt:1989-2112).
- U1.5 voice parity: speakDaySummary + per-card SpeakButton speak the same words, and refuse buy advice on cached data (InterdayViewModel.kt:1342-1389, 2000-2006).
- U1.6: freshness shown in words — 'Updated 12:45pm • Market open' green dot (HomeScreen.kt:706-808; screenshot 13).
- B0.3b: practice money labelled amber with 'PRACTICE profit today' prefix and 'You are in PRACTICE mode — only fake money is used' (TodayPnlBar.kt:54-69; HomeScreen.kt:1422-1424; screenshot 13).

### intraday-tab — 12 pass-items
- U7.3: C12a stale-entry flip is implemented on both cards — MasterSignalCard shows 'This chance passed — wait for the next one' + hides all buy controls (TradingScreen.kt:1596-1629), CompactSignalCard shows 'PASSED' / 'Chance passed — do not buy now' (TradingScreen.kt:1963,1995-2021).
- U7.4: setup shown as words Strong/Okay/Weak, raw /100 kept behind 'See more' — verdictWord (TradingScreen.kt:103-107) + honest caption 'This means the setup quality — not a promise of profit' (1693-1698); bands match TradingSignal.confidenceMeaning (TradingSignal.kt:125-134).
- C13: no calculator anywhere on the tab — quantity is auto ('Shares to buy: N', TradingScreen.kt:1721-1723) from the single stored dailyCapital; no money-input field exists on this screen.
- C9: exactly one market banner (NseMarketStatusBanner, TradingScreen.kt:424-432); the old header open/closed pill was removed (188-201 comment).
- C10: SafetyCheckCard gives the single yes/no answer with plain reason and India-VIX 'Market mood' translated to calm/a bit jumpy/very jumpy (TradingScreen.kt:1058-1099) — matches screenshot 14.
- C11: Daily/Weekly loss banners render on top and set buyBlockReason so the Groww button is truly disabled (TradingScreen.kt:343-346,498-519); weekly words used when WEEKLY breached (E3e).
- C12: master card carries the full B3 instruction set on the default view — action badge, price, Buy-at, Shares, Stop, Target, profit-after-charges (net of intraday round-trip cost), sell-plan per target, INTRADAY Groww button (TradingScreen.kt:1700-1817).
- B4/H8 blocks are hard, not advisory: market-closed, 9:15-9:30 opening window, 3:00 PM hard block, loss-limit and capital-path all funnel into buyBlockReason which greys the button with a one-line reason (TradingScreen.kt:334-360; RiskGuard.kt:92-131,212-217).
- NO-TRADE honesty (E1b/B9): screenshot 14 shows the amber shield 'NO TRADE' + 'No stock passed the safety checks right now' + 'No trade is also a good choice' on a down day (-0.59%) — honest protective empty-state (NoTradeCard, TradingScreen.kt:2036-2081).
- B2 stale honesty: cached quote shows red 'Old price. Do NOT buy or sell on this' and disables the Groww button while market is open (TradingScreen.kt:1644-1654,1764-1769); FreshnessLine shows plain 'Updated HH:MM' / 'Old price - from X minutes ago' (856-915).
- I9: rupee amounts use Indian grouping via formatIndianRupees (TradingScreen.kt:53-54); NIFTY shown as % not ₹ in banner (1467-1471) and screenshot 14.
- C14/C16: 'More chances today (N)' collapsed behind one tap (TradingScreen.kt:690-736) and an instant-verdict stock search (1101-1135) are present without competing with the one decision.

### protection-chain — 14 pass-items
- E1: TradeWatchService runs a ~90s Yahoo poll while a REAL position is open and self-stops when none remain (TradeWatchService.kt:79-92,236) — foreground half of 'both, auto'.
- E1: ScannerWorker is the ~15-min WorkManager backup, gated on marketCalendar.isMarketOpen(), and actually re-checks stop-loss/target for the real tracked_trades table plus the daily-loss limit (ScannerWorker.kt:42-56,67-97,109-137,200-201).
- Feed self-start after restart: the FGS loop starts the SmartAPI feed itself for exactly the open-trade symbols when keys are configured, idempotent and guarded, so real-tick protection resumes without opening the app; with no keys it correctly stays Yahoo-only (TradeWatchService.kt:97-112).
- B0.4 ALERT-ONLY: the service, both receivers and the worker only post notifications + speak TTS — no order place/cancel path anywhere (TradeWatchService.kt:126-158, SquareOffAlarmReceiver.kt:38-43, ScannerWorker.kt:80-96).
- C15/C15a vibration+voice: alertStopLossLive/alertTargetLive/alertSquareOff each call vibrateStrong() (waveform, VIBRATE permission declared) and post a plain-words heads-up on the HIGH-importance ALERTS channel, paired with English TTS (NotificationManager.kt:78-128).
- C15a repeat: stop-loss and target re-buzz on consecutive watch cycles up to MAX_REPEAT_ALERTS=3, with counters pruned for closed trades and re-armed on price recovery (TradeWatchService.kt:86-88,133-157,239).
- C15a 3:15 alarm: SquareOffAlarmReceiver fires for open REAL INTRADAY trades only, vibrates+speaks plain words, and reschedules daily; the alarm is armed on every app launch (MainActivity.kt:90), after boot (BootReceiver.kt:30) and in its own finally block (SquareOffAlarmReceiver.kt:45), with exact-alarm permission checked and an inexact 3:05-3:15 window fallback (SquareOffAlarmReceiver.kt:73-93).
- Boot resilience: BootReceiver handles BOOT_COMPLETED + QUICKBOOT_POWERON, always re-arms the 3:15 alarm, and restarts the FGS only when open REAL trades exist (BootReceiver.kt:25-53).
- Practice money never buzzes: real-only filtering is consistent across the FGS (getOpenOnce(false)), boot restart, square-off (!isPractice) and scanner (practice=false) (TradeWatchService.kt:81, BootReceiver.kt:38, SquareOffAlarmReceiver.kt:37, ScannerWorker.kt:70).
- B0.2d battery: BatteryGuard.isProtectionAtRisk + requestIntent prefer the one-tap system exemption dialog ONLY when the app actually holds REQUEST_IGNORE_BATTERY_OPTIMIZATIONS, falling back to the settings list, and the risk is surfaced as a plain-words warning card (BatteryGuard.kt:35-67, HomeScreen.kt:1515-1516).
- dataSync 6h cap handled honestly: both onTimeout overloads stopSelf() and post a visible 'watch paused — reopen the app' notification instead of dying silently (TradeWatchService.kt:174-213).
- Manifest is correct and minimal: FOREGROUND_SERVICE_DATA_SYNC matches foregroundServiceType=dataSync, POST_NOTIFICATIONS/VIBRATE/SCHEDULE_EXACT_ALARM/USE_EXACT_ALARM/RECEIVE_BOOT_COMPLETED/REQUEST_IGNORE_BATTERY_OPTIMIZATIONS all declared, both receivers exported=false with the boot intent-filter present (AndroidManifest.xml:5-16,56-75).
- B0.2b/B0.3a: pending-order marker persists in DataStore (survives process death, 1h staleness) and close() subtracts round-trip charges via roundTripCostRs so P/L, loss limits and the weekly report match Groww (TradeTrackerRepository.kt:118-172,186-244).
- Honesty of the trust/learning surfaces: OutcomeRecorder needs >=5 real samples before any 'right X of Y' line, excludes practice, and is fail-safe (OutcomeRecorder.kt:106-130,182-201,219).

### mutual-fund — 11 pass-items
- U6.1 (one-fund-first): 'Start here — the simplest fund' shows UTI Nifty 50 Index Fund as THE single answer with a plain 'Why: Simplest start — owns all 50 biggest companies for a tiny fee', alternatives hidden behind 'See 4 more safe choices' (screenshot 17; MutualFundsScreen.kt:346-397, MfStartHereHeader:529-551).
- U6.2 / C18: projection is honestly labeled — collapsed card 'in 5 years it could become about ₹73,745 — an estimate, not a promise' (screenshot 17), and the expanded tool ends with 'This is only a guess, not a promise' + 'Past returns don't promise the future' (lines 1301-1314).
- U6.6 / I2: no user-facing 'NAV' anywhere — card shows '₹168.67 / Price per unit • 22-07-2026' (line 939), loading shows 'Getting today's price first…' (line 787); every 'NAV' string is confined to code comments/data-field names (grep confirmed).
- U6.5 / C19: risky funds get the Groww start button hard-disabled (enabled = !isRisky, lines 1393-1400) plus a red Warning row and 'Not for beginners - pick a safer fund above.' (lines 1349-1367, 1417-1425); the risky red badge shows on the card too (MfRiskyBadge:1104-1128).
- C19 hard filter: the curated beginner list is flexi-cap/large-cap/index/balanced only — small-cap and mid-cap are deliberately excluded (MutualFundRepository CURATED_FUNDS:95-120; EXCLUDED_PLAN_WORDS drops IDCW/dividend/regular variants).
- C20: exactly 3 how-to lines + the 'never do' line — 'How to start a SIP (3 steps)' with steps 1-3 and bold 'Never stop your SIP when the market falls…' the #1 mistake (lines 587-605; screenshot 16), with a 🔊 read-aloud button (I5).
- C20 date guidance: the app suggests a date ('Pick a date just after your salary day — like the 1st or 5th', lines 596-597 & 1376) and defaults SIP tracking to Day 5 (line 1448).
- C20a / U6.4: SipReminderWorker is actually scheduled — enqueueUniquePeriodicWork(KEEP) in MainActivity.kt:75-79, runs daily fully offline, refreshes the same fixed notification id, and fires on-or-after SIP day via SipRecord.isDueOn (SipStore.kt:46-47).
- C20a growth line honesty: growth uses real fetched NAV only (units = amount ÷ confirm-day NAV, MutualFundsViewModel.confirmSipPaid:207-224); empty state 'No payments counted yet' (line 799); late confirms flagged 'approx - one payment was confirmed late' (lines 818-825); confirm button disabled until NAV loads (line 780).
- D3: the NIFTY 50 index fund (UTI Nifty 50 Index Fund) is the default #1 recommendation on the tab, matching the 'index is the answer' rule (CURATED_FUNDS first entry:100-103).
- Stale-data protection: NAVs older than 7 days badge 'Old data (N days)' (lines 945-955), suppress the projection and Start button (lines 979, 1021-1027); disk-cache list is labelled 'Yesterday's prices' with tap-to-retry, and no endless spinner (6s → retry, lines 180-188, 480-526).

### guide-moreways — 11 pass-items
- U8.1: All 6 earning-order lessons present (stop-loss, why-these-stocks, SIP, targets, common-mistakes, scam), each ending with a 'How this makes you money' line — LearningScreen.kt:304-365.
- U8.1: Scam lesson carries the red 'PROTECTS REAL MONEY' tag and tinted card — LearningScreen.kt:356-364,397-411; confirmed in shot 18_guide_top.png.
- U8.2: Every lesson has its own 🔊 listen button and TTS readiness is probed with an honest 'Voice not ready' toast fallback — LearningScreen.kt:430-436, LearningViewModel.kt:60-79.
- C21/I5: Spoken lesson = title + body + moneyLine, matching the on-screen text exactly (read-aloud parity) — LearningScreen.kt:121-123.
- U8.3/C23: Guide is a separate optional tab that never blocks/delays earning; Advanced is labelled 'you never need this to use the app' — LearningScreen.kt:154-158.
- C24: A clearly-separated 'Advanced' area exists and hosts the 'More ways to earn' entry into PART D; glossary of market words is collapsed by default so a beginner never meets jargon — LearningScreen.kt:145-237.
- D1: Live gold & silver ₹ prices via free Yahoo (GOLDBEES.NS/SILVERBEES.NS), verdict = price vs its own 200-day average (falls back to 50-day honestly), one-tap 'Buy in Groww' hand-off — MoreWaysViewModel.kt:101-112,156-181.
- D1: Monthly gold amount pre-computed from the user's stored capital (~5% floored to ₹100), shown only when the verdict already says gold is cheap — MoreWaysViewModel.kt:135-140.
- D4: US stocks are strictly watch-only — never isGood/BUY, button says 'See in Groww' not 'Buy', honest 'buying US stocks needs a special account' line — MoreWaysScreen.kt:157-173,300-306; MoreWaysViewModel.kt:113-125,170-175.
- D3: Index SIP card uses shopkeeper words ('ready-made basket of India's 50 biggest companies'), keeps the term 'index fund' behind See more (I2), and offers a Groww start button — MoreWaysScreen.kt:193-226.
- B9/I6: More Ways has an honest error state with a working Retry button — never a dead end — MoreWaysScreen.kt:136-152.

### settings-voice-keys — 16 pass-items
- E5: Advanced raw-% fields hidden behind a collapsed 'Advanced — exact percent numbers' expander (default closed), SettingsScreen.kt:73,190 — beginner never sees raw numbers.
- E5: Carefulness offered as plain-word presets 'Safe'/'Normal' with honest ₹ money-lines and no raw %, SettingsScreen.kt:302-342 (screenshot 20).
- E5: Safe automatic defaults (Normal = 1%/3%/8% risk/daily/weekly, UserSettingsStore.kt:51-54) — a beginner needs never open Settings.
- E5: Angel One key card is placed LAST and clearly optional ('The app works fully without this'), SettingsScreen.kt:276-288 (screenshot 25).
- A4 amendment: master 'Voice' switch shown FIRST, 'Speak alerts in Hindi' second; screen copy stays English, SettingsScreen.kt:409-410,443-529 (screenshot 24).
- A4/E2: Master Voice OFF makes the app fully silent — every public speak method gates on isVoiceOn() first, HindiTtsManager.kt:142,158,178,185,197,209 (default TRUE).
- A4 amendment: Hindi switch writes 'voice_hindi', re-read on every sentence so the flip is instant and survives restart, HindiTtsManager.kt:57-70,139.
- E6: SmartAPI credentials encrypted with hardware-backed Keystore AES-256-GCM before disk; keys never in code; legacy plaintext migrated then wiped, SmartApiStore.kt:89-116,118-139,147-191.
- E6/B0.4: Angel One card states 'This login only READS live prices - the app can never place, change, or cancel orders', SmartApiSettingsCard.kt:78-82 (screenshot 25).
- E6: Broker keys deliberately excluded from the backup file (documented security decision), DataBackup.kt:52-58.
- B8 free-forever: 'The app runs fully on free data — no keys and no payment needed'; Angel One purely optional; dead Alpha Vantage/NewsAPI/RapidAPI key fields removed, SettingsScreen.kt:247-266 (screenshots 24/25).
- B7b/U3: Check-in offers tap-ready amounts (1,000/5,000/10,000/25,000/50,000) + optional 'type your own' + 'Not today' + Save; title 'How much will you invest today?', screenshot 07.
- B7b/U3.2: Check-in asked once per TRADING day only — never twice, never on weekends/NSE holidays; yesterday's amount carries over on skip, InterdayViewModel.kt:457-474.
- E5: Alerts section shows plain on/off state line and a real 'Send test alert' fired on the SAME channel real alerts use, SettingsScreen.kt:404-433,548-568.
- E6/U9.5: 'Recent problems' surfaces the local crash log, stays on-device, leaves only if the user taps Share, SettingsScreen.kt:604-700.
- Data export/import: BackupViewModel/DataBackup fully implemented but intentionally NOT wired into any Settings UI (only self-referenced); consistent with locked C21a 'journal and settings start fresh', honestly stated in the About card (screenshot 24) — not a violation.

### honesty-moneymath — 14 pass-items
- H2: Scoring is 100% real-data — MA(200/50), real 14-day Wilder RSI only, volume-vs-avg, 52w range, momentum, market-trend, real per-sector rotation; missing factors add NO points AND grow no denominator (SignalEngine.kt:106-211).
- H3: confidence = 30 + points/maxPossible*67, capped 82/88/97 when MAs are missing, and MIN_CONFIDENCE 70 floor rejects below (SignalEngine.kt:215-225).
- B6/RSI: wilderRSI() returns null when history < 15 closes; RSI is never synthesized from one day and the word 'RSI' never reaches the screen (SignalEngine.kt:127-140,363-382).
- H3 honesty copy: confidenceMeaning states 'score X out of 100 ... NOT the chance of profit and NOT a guarantee' and is actually shown in the card (TradingSignal.kt:125-134; TradingScreen.kt:1827).
- B6 beginner-safe: isBeginnerSafe only when action==BUY && LOW — a short-sell can never be labelled beginner-safe (SignalEngine.kt:337).
- H5 charges gate: setups are rejected when charges make net profit <=0 or net reward:risk < 1.8, computed before any green target is shown (SignalEngine.kt:258-261).
- Ledger conservatism: judge() checks stop FIRST, both-touched => FAIL, never claims an unprovable win, and rows stay PENDING when no quote or close<=0 (SuggestionLedger.kt:248-277,210-215) — matches RULE_TEXT verbatim as shown in screenshot 27.
- Ledger integrity: only BUY/SELL are recorded (WAIT excluded), idempotent per (date,symbol), two physically separate CSV files that can never mix (SuggestionLedger.kt:114-159).
- LedgerEvaluationWorker: judges only after 15:35 IST on a real trading day, skips weekends/holidays, and leaves rows PENDING when Yahoo is down — verdicts never invented (LedgerEvaluationWorker.kt:47-98).
- LedgerViewModel: read-only render, never computes PASS/FAIL itself, count-only summary with no averages/percentages (LedgerViewModel.kt:116-147).
- H13 auto-caution IS wired: refreshCautionMode requires >=6 of last 10 REAL closes and win-rate < 30%, then applyCautionCap keeps only the single top BUY with an honest 'system is not doing well' line (InterdayViewModel.kt:635-663).
- Trust line (E3a/H13): TrustLineProvider only speaks from a real backtest with >=5 simulated trades, says 'last year' only when the scan truly covered one else 'recently', null => UI shows nothing, and is wired null-safe into HomeScreen (TrustLineProvider.kt:67-102; HomeScreen.kt:1046,2054).
- OutcomeRecorder trust line: only from >=5 REAL outcomes, practice trades never recorded, null => nothing; recentWinRate null below 5 (OutcomeRecorder.kt:106-151,219).
- E1c after-loss: afterLossInfo states the REAL realized net loss, never invents a 'saved Rs Y' figure, returns null on profit/breakeven, and formats the amount with Indian grouping (OutcomeRecorder.kt:182-206).

### jargon-format — 10 pass-items
- U9.1/I2: Signal engine translates every internal jargon token before it reaches the screen — RSI→"the stock looks tired"/"Risen too fast — it may fall back" (SignalEngine.kt:137-138), VWAP→"Price is above/below today's average" (IntradayScorer.kt:225-226), VIX/volatility→"very jumpy"/"a bit jumpy"/"calm" (TradingScreen.kt:1081-1083, RiskGuard.kt:163), BULLISH/BEARISH→"rising"/"falling" (TradingScreen.kt:1886).
- U9.1/I2: Cloud-AI free text is passed through a JARGON_SUBSTITUTES filter before display — RSI→"the stock's energy meter", ATR→"the usual daily price swing", VWAP→"the day's average price", NAV→"price per unit", T1/T2/T3→"Target 1/2/3", square-off→"the broker's automatic sell at 3:20", price band→"share price range" (StockResearchViewModel.kt:93-110).
- U9.1/I2: Bare "T1/T2/T3" never shown — sell plans spell out ordinals/"Target 1" (HomeScreen.kt:76-79 ordinalLabel, TradingScreen.kt:89,1783); the old RecommendationCard that rendered "T2"/"RR" was deleted (CommonComponents.kt:208-211).
- U6.6/I2: Mutual-fund per-unit price is labeled "Price per unit • {date}", never "NAV", on the user card (MutualFundsScreen.kt:938-939); the raw NAV value is only shown with the ₹ + shared formatter (line 933).
- I9/U9.2: formatIndianRupees() implements TRUE lakh/crore grouping (CommonComponents.kt:70-124) and is the formatter for every money amount on the Stock tab (HomeScreen fmtRs, line 69 + budget/price/stop/target/profit at 1333,1372,2060,2071,2075) and the Intraday tab (TradingScreen.kt:53-54).
- I9/U9.2: NIFTY is shown as points, never with a ₹ sign — HomeScreen.kt:739-741 prints the index value/change with no ₹ prefix; confirmed on screen (02_stock_top "NIFTY 50 23,887.30 -108.95 (-0.45%)", 14_intraday_top "NIFTY -0.59% today").
- I9/U9.3: Red is used only for down/loss/danger (NIFTY -0.45% red in 02, MyStockCard down = RedPrimary CommonComponents.kt:484, stop-loss/HIGH-risk red) and amber only for caution/practice (PRACTICE badge CommonComponents.kt:471-479, NO-TRADE shield in 14_intraday_top, alerts-off banner, REAL-MONEY toggle card in 02) — meanings kept consistent.
- I2: The jargon tokens that exist in the codebase (RSI/ATR/VWAP/breadth/regime/bullish) live only in variable names, scoring constants and comments — none appear inside a user-facing Text() string.
- I2: DirectionBadge ("LONG"/"SHORT") and ConfidenceBadge ("HIGH 91%") in CommonComponents.kt:167-206 are dead code — grep found zero call sites — so that jargon is never rendered.
- U9.1/I2: The Guide glossary defines each market term in one line of plain English (LearningScreen.kt:652-689), consistent with U9.1's "unless translated" clause.

### realdata-freshness — 13 pass-items
- B8: All data sources are free — Yahoo Finance query1/query2 (IntradayRepository.kt:55-58), NSE official live-analysis endpoints (NseRepository.kt), Google News RSS, and an OPTIONAL free-account Angel One SmartAPI; no paid API is referenced anywhere.
- B8: Angel One layer is strictly opt-in and fail-safe — fetchIntradayReads returns emptyMap() when not configured / market closed / any error (IntradayRepository.kt:485-537) and SmartApiFeed.runFeed returns immediately if creds incomplete (SmartApiFeed.kt:155), so every feature runs at full power on free Yahoo data with zero keys (matches this device's no-keys state).
- No dummy/hardcoded prices: parseChartResponse and parseV7Quotes both reject price<=0 (IntradayRepository.kt:1461-1462, 1204-1205); NSE fallback uses only real ltp/lastPrice (1079-1083); repo-wide grep for dummy/mock/sample/placeholder/Random found no fabricated-price path — all 'placeholder' hits are honest 'data unavailable' flags or search-hint text.
- Honest unavailable state, never a fake number: defaultHealth() returns niftyChange=0.0 with isAvailable=false so the UI shows 'data unavailable' rather than a fake 0.00%/SIDEWAYS (IntradayRepository.kt:2284-2287).
- B2 ~30s refresh: startAutoRefresh runs every 30s during market hours (30min when closed), foreground-gated per E3d (InterdayViewModel.kt:1002-1019); fetchIndices/signals suspend while backgrounded to respect mobile data.
- B2 one-tap manual refresh always available: refresh icon present in the top app bar on both Stock (02_stock_top.png) and Intraday (14_intraday_top.png) tabs, wired to refresh()->launchFetch (InterdayViewModel.kt:807-817).
- B2 honest 'last updated': fresh path stamps lastUpdated with the actual clock time (InterdayViewModel.kt:922); screenshots show 'Updated 12:37 pm • Market open' (02) and 'Updated 12:46 pm' (14), both matching the capture time.
- B2 stale label exists in plain words: staleAgeLabel produces 'Old price - from X minutes ago' on cold-open (InterdayViewModel.kt:795,1212-1226) and FreshnessLine/CompactStatusHeader render it in amber when cached (TradingScreen.kt:882-903, HomeScreen.kt:696-710).
- B2/E2 voice refuses stale: speakPick and speakDaySummary both short-circuit when isUsingCachedData and speak a refusal instead of buy/sell prices (InterdayViewModel.kt:1313-1316, 1346-1348).
- E3 caching / instant-open: loadCachedThenFetch renders cached signals + screener + saved daily picks immediately, then fetches live (InterdayViewModel.kt:766-805); cacheSignals/cacheScreener/saveDailyPicks persist to SharedPreferences with a cache_ts timestamp (IntradayRepository.kt:2209-2340).
- Live data confirmed refreshing (real, not static): NIFTY 50 moved from -0.45% at 12:37 (02_stock_top.png) to -0.59% at 12:46 (14_intraday_top.png), consistent with LEGEND's ~30s live-refresh observation.
- Market open/closed shown from the IST clock+holiday calendar rather than the dead v7 marketState field, so 'Market open' is correctly shown at 12:37/12:46 instead of a false 'closed' (TradingScreen.kt:362-370, screenshots 02/14).
- NSE fallback engages only when Yahoo returns <20 quotes (Yahoo down) and never on the happy path, and is fail-safe to the existing cached/stale behavior (IntradayRepository.kt:1043-1048).

### neverempty-blocking — 12 pass-items
- U5.1/C8: No-internet full-screen popup fires ONLY when there is no cache at all — offlineBlocked = lastFetchFailed && !isUsingCachedData && lastUpdated.isEmpty() && signals.isEmpty() (HomeScreen.kt:162-165); forced (not outside-dismissible), speaks the instruction once (169-171), friendly copy + 'Turn on Internet'/'Try again' buttons.
- U5.3/B2: With stale cache the Stock decision REFUSES to decide on old prices — 'No internet — I won't give a decision on old prices.' + Try-again + voice refusal 'No internet. I won't decide on old prices.' (HomeScreen.kt:1803-1824).
- U2.2/I7: The Intraday Groww buy button is REALLY disabled (greyed) with a one-line reason when blocked — GrowwActionRow(enabled = netBlock == null, disabledReason = netBlock) (TradingScreen.kt:1806-1814) → GrowwAction.kt:136,149 ('Buying is off right now'),154-157 (reason shown).
- I7: buyBlockReason covers every block with plain words — market closed ('opens …'), 9:15-9:30 opening window, weekly limit ('start fresh next week'), daily limit, RiskGuard block, and all three capital-path cases (TradingScreen.kt:334-360).
- U9.5/I6: No error codes or stack traces reach the user — exceptions are mapped to 'No internet. Turn it on and try again.' / 'Taking too long. Try again.' / 'Could not get prices. Try again in a minute.' (InterdayViewModel.kt:980-995); research error is 'Could not fetch data for X. Check symbol.' (IntradayRepository.kt:857); raw .message only ever goes to Log, never the UI.
- B7c: Stock-tab market-closed screen is never empty — 'Market closed — opens …. Nothing to do now.' + live countdown 'opens again in about X hours/days' + 'Your SIP keeps working — see the Mutual Fund tab.' + a clock-chosen contextual line (US-at-night vs Gold/SIP) (HomeScreen.kt:1747-1799).
- B9/U1.4: Stock-tab no-trade says WHY and points to what IS possible — 'Don\'t buy today — no strong opportunity. Saving money is also earning.' + 'Grow your money safely with a monthly SIP — tap Mutual Fund below.' (HomeScreen.kt:1925-1935); unaffordable/capital-path branches also route to SIP (1899-1923).
- B9: Daily/weekly loss-limit cards replace the decision AND keep a next step — 'Your SIP keeps working — saving is also earning.' (HomeScreen.kt:1578-1581; LossLimitCard swap at 1000-1013).
- Market-state truth = RiskGuard.isTradingHoursNow() everywhere; the dead v7 marketState field is never used for open/closed — HomeScreen.kt:143,891; TradingScreen.kt:333,369 (effectiveMarketState); StockResearchScreen.kt:322; state.marketClosed itself = !isTradingHoursNow() (InterdayViewModel.kt:929,977).
- Search is NSE/BSE-first — Indian listings sorted ahead of foreign ones (IntradayRepository.kt:992) and the top result carries the '⭐ Best match — tap this' star pointing at TCS.NS/NSE (shots/29_search_tata.png; HomeScreen.kt:594-601).
- U4.5: Alerts-at-risk banners are pinned on every tab and RE-CHECK on resume — NotificationsOffBanner + BatteryProtectionBanner in the sticky header (HomeScreen.kt:294-302; TradingScreen.kt:215), never one-time-dismissible.
- C9: Intraday has exactly ONE market banner with open/closed + NIFTY% + countdown ('Market is Open • closes in 2h44m • LIVE'), driven by effectiveMarketState (TradingScreen.kt:424-432,1454-1504; shots/14_intraday_top.png).
